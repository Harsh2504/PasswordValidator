namespace PasswordValidator;

public class PasswordValidator
{
    public void Validate(string password)
    {
        // evaluate rules
        // collect violations
        // compute validity & strength
    }
}
